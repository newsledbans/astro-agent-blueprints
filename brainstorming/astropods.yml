# yaml-language-server: $schema=https://astropods.ai/schema/package.json
spec: package/v1
name: brainstorming

agent:
  build:
    context: .
    dockerfile: Dockerfile

models:
  openai:
    provider: openai

dev:
  interfaces:
    messaging:
      adapters: [web]
